import { AppLayout } from '@/components'
import styles from './WalkLog.module.scss'

export const WalkLog: React.FC = () => (
    <AppLayout>
      <h2 className={styles.greetings}>Walk Log</h2>
    </AppLayout>
  )

